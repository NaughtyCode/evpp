add_executable(main main.cc)
set_target_properties(main PROPERTIES CXX_STANDARD 23 CXX_STANDARD_REQUIRED ON)
target_link_libraries(main ServerEngine)
if(WIN32)
    target_link_libraries(main ws2_32)
endif()
