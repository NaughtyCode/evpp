# Changelog: Core Validation Fix

## [Unreleased]

### Added
- (pending) Wire ConfigValidator::Validate() into Load() path
- (pending) Wire ConfigValidator::Validate() into Reload() path
- (pending) Add ValidateServerConfig() method
- (pending) Add ValidateClientConfig() method
- (pending) Add cross-field validation (target_fps vs interval_ms)
- (pending) Add sandbox_level enum validation
- (pending) Add error_on_missing_keys warning diagnostics
- (pending) Unit tests for all new validation rules
- (pending) Integration test for Load-then-Validate flow
